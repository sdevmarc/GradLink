export interface IHeadSectionChildren {
    title: string
    description: string
}

export interface ISidebar {
    children: React.ReactNode
}

export interface ISidebarNavs {
    title: string
    link: string
}